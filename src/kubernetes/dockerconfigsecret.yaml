apiVersion: v1
kind: Secret
metadata:
  name: dockerconfigjson
  namespace: cinemaabyss
# Public images: no registry credentials needed. This is a valid but empty
# docker config ({"auths":{}}), so imagePullSecrets resolves and pulls stay anonymous.
data:
  .dockerconfigjson: eyJhdXRocyI6e319
type: kubernetes.io/dockerconfigjson
